import ForgetPassword from "../components/user/ForgetPassword";
import NavBar from "../components/NavBar";

const ForgerPass = () => {
  return (
    <div className="h-screen overflow-hidden">
      <NavBar />
      <ForgetPassword />
    </div>
  );
};

export default ForgerPass;
